# Targets chezmoi should delete from $HOME if present.
# The monolithic ~/.zshrc was replaced by $ZDOTDIR (~/.config/zsh) config.
.zshrc
# Catppuccin was replaced by Kanagawa Dragon; lazy.nvim treats any spec left in
# the plugins dir as live, so a stale copy would reinstall the old colorscheme.
.config/nvim/lua/phihdn/plugins/catppuccin.lua
